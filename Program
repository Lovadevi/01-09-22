#include <stdio.h>
int fact(int n)
{
    if(n==1)
      return n;
    else 
      return(n*(fact(n-1)));
}
int main()
{
    int n;
    printf("enter n value");
    scanf("%d",&n);
    printf("%d",fact(n));
    return 0;
}
